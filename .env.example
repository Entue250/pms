# Copy this file to src/main/webapp/WEB-INF/.env and fill in your own values.
# WEB-INF/.env is never served over HTTP by the servlet container, and is
# loaded at startup by com.pms.util.EnvLoader.
DB_URL=jdbc:mysql://localhost:3306/PMS
DB_USER=root
DB_PASSWORD=
